# Template for the MongoDB connection string. Copy this file to
# mongodb_config.py (which is gitignored) and fill in your real URI.
# NEVER commit the real URI to the repository.
CONNECTION_URI = 'mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?appName=<app>'
